# Next action

Wait until APK is installed.

Then in Termux run:

```bash
cd $HOME/pokeclawte/termux-bridge
./direct-am-test.sh "how much battery left"
```

If PokeClaw opens, run:

```bash
./start.sh
./test.sh
```
